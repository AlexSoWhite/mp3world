<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
<data>
    <variable
        name="songCount"
        type="Integer" />
    <variable
        name="playlistCount"
        type="Integer" />
    <variable
        name="albumCount"
        type="Integer" />
    <variable
        name="artistCount"
        type="Integer" />
    <variable
        name="favouriteCount"
        type="Integer" />
</data>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.MainActivity"
    android:background="@drawable/background_gradient"
    android:paddingTop="50dp">

    <LinearLayout
        android:id="@+id/main_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.3">

        <include
            android:id="@+id/all_songs"
            layout="@layout/main_menu_item"
            bind:description="@{&quot;Мои песни&quot;}"
            bind:count="@{songCount.toString()}" />

        <include
            android:id="@+id/playlists"
            layout="@layout/main_menu_item"
            bind:description="@{&quot;Мои плейлисты&quot;}"
            bind:count="@{playlistCount.toString()}" />

        <include
            android:id="@+id/artists"
            layout="@layout/main_menu_item"
            bind:description="@{&quot;Исполнители&quot;}"
            bind:count="@{artistCount.toString()}" />

        <include
            android:id="@+id/albums"
            layout="@layout/main_menu_item"
            bind:description="@{&quot;Альбомы&quot;}"
            bind:count="@{albumCount.toString()}" />

        <include
            android:id="@+id/favourite"
            layout="@layout/main_menu_item"
            bind:description="@{&quot;Избранное&quot;}"
            bind:count="@{favouriteCount.toString()}" />

<!--        <include-->
<!--            android:id="@+id/statistics"-->
<!--            layout="@layout/main_menu_item"-->
<!--            bind:description="@{&quot;Статистика&quot;}"-->
<!--            bind:count="@{&quot;&quot;}" />-->
    </LinearLayout>

    <include
        android:id="@+id/loader"
        layout="@layout/loader"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <com.google.android.exoplayer2.ui.StyledPlayerControlView
        android:id="@+id/player_control_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:controller_layout_id="@layout/player_control_view"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>